---
import type { GetStaticPaths } from 'astro';
import { CULTURE_PAGES } from '~/constants/Culture';
// Layout
import Culture from '~/layouts/Culture.astro';

export async function getStaticPaths() {
  return CULTURE_PAGES.map((page) => {
    return {
      params: page,
    };
  });
}
---

<Culture />
